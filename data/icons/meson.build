sizes = ['32', '48' , '64', '128']
themedir = get_option('datadir') + '/icons/hicolor/'

foreach size: sizes
    install_data(size + '/tranquil.svg', install_dir: themedir + size + '/apps/')
endforeach
